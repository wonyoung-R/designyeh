# DesignYEH Website

**DesignYEH**는 창의적인 디자인과 혁신적인 기술로 고객의 디지털 경험을 향상시키는 웹 에이전시의 공식 웹사이트입니다.

![DesignYEH Preview](public/images/preview.png)
*(Note: Add a preview image if available)*

## 🛠 Tech Stack

- **Framework**: [Next.js 15+](https://nextjs.org/) (App Router)
- **Language**: TypeScript
- **Styling**: [Tailwind CSS](https://tailwindcss.com/)
- **UI Components**: [Shadcn UI](https://ui.shadcn.com/)
- **Animation**: [Framer Motion](https://www.framer.com/motion/)
- **Icons**: [Lucide React](https://lucide.dev/)
- **Database**: [Supabase](https://supabase.com/)

## 🚀 Getting Started

### 1. 프로젝트 클론

```bash
git clone https://github.com/wonyoung-R/yeh.git
cd yeh
```

### 2. 패키지 설치

```bash
npm install
```

### 3. 환경 변수 설정

프로젝트 루트에 `.env.local` 파일을 생성하고 Supabase 연결 정보를 입력합니다.

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 4. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000)으로 접속하여 확인합니다.

## 🗄 Database Schema

이 프로젝트는 Supabase를 데이터베이스로 사용합니다.

### Portfolios Table

포트폴리오 데이터를 저장하는 테이블입니다.

```sql
create table public.portfolios (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  title text not null,
  description text,
  details text[],
  image text,
  link text,
  tech text[]
);
```

*상세 스키마 및 초기 데이터는 `supabase_schema.sql` 파일을 참고하세요.*

## 📂 Project Structure

```
.
├── public/              # 정적 파일 (이미지, 파비콘 등)
├── src/
│   ├── app/             # Next.js App Router 페이지
│   │   ├── about/       # 회사 소개 페이지
│   │   ├── contact/     # 연락처 및 문의 페이지
│   │   ├── portfolio/   # 포트폴리오 목록 페이지
│   │   └── page.tsx     # 메인 페이지
│   ├── components/      # 리유저블 컴포넌트
│   │   ├── ui/          # Shadcn UI 컴포넌트
│   │   └── ...
│   └── lib/             # 유틸리티 함수 및 라이브러리 설정 (Supabase 등)
└── ...
```

## 🎨 Features

- **반응형 디자인**: 모바일, 태블릿, 데스크탑 등 모든 기기에서 최적화된 화면 제공
- **스크롤 애니메이션**: Framer Motion을 활용한 부드러운 스크롤 등장 효과
- **다크 모드 지원**: 시스템 설정에 따른 다크 모드 UI (기반 마련)
- **실시간 데이터 연동**: Supabase를 통한 포트폴리오 데이터 관리 및 실시간 반영

## 📝 License

This project is private software of DesignYEH.
